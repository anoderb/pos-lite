feat(qris): UI QRIS dinamis + approval manual di POS, pengaturan & dashboard

pos-engine.jsx:
- Hapus metode transfer (METODE_LABEL/BADGE/ICON + metodeTersedia + 2 array
  metode inline + import Building2 + fallback label struk)
- Alur QRIS pending: checkout qris -> panel QR (api.qrserver.com dari
  qris_payload) + tombol Approve (-> struk) / Cancel (wajib alasan -> batal)
- State qrisPendingTx/showQrisPending/qrisActionLoading dll; owner-nav-hide
  saat panel pending; metodenya = handleApproveQris/handleCancelQris
- Riwayat: filter [Semua | Pending] + badge status (Sukses/Pending/Batal) +
  klik no trx buka struk (openStrukRiwayat) + pagination dinamis
  (reloadRiwayatPage); badge pending count (riwayatPendingTotal)
- Gate metode qris: tokoPay.qris_aktif && qris_status==='valid' (bukan qris_url)

pengaturan/page.jsx:
- Hapus section Rekening Bank + state transfer/bank
- Tambah section QRIS Dinamis: textarea string QRIS, badge status
  (Valid/Invalid/Belum Diatur), info merchant (nama/kota/merchant-id/tipe),
  tombol 'Simpan & Validasi QRIS' (PUT /owner/toko/qris)
- fetchQrisStatus() saat mount (GET /owner/toko/qris/status)

dashboard/DashboardContent.jsx:
- Kartu 'QRIS Pending' (kuning) dengan count dari /owner/laporan/pending +
  link approve ke /owner/pos; grid KPI jadi lg:grid-cols-4
